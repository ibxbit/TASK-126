// Vitest global setup — registers jest-dom matchers so individual
// test files don't have to import "@testing-library/jest-dom" at the
// top of every file. Referenced from vite.config.ts → test.setupFiles.

import "@testing-library/jest-dom";
